# Legacy artifacts

The original notebook, writeup PDF, and an earlier project-analysis note.
They predict next-day *price levels* and report R² ≈ 0.997 — the trap the
current experiment exists to replace.

They are kept for history only. Run `python run_experiment.py` from the
repo root for the numbers the main README reports.
