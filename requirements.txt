# Runtime dependencies -- every package here is imported by the code, and both
# code paths (run_experiment.py and train.py/predict.py) are covered by this set.
#
# Verified against this exact set on Python 3.11.15:
#   numpy 2.3.5, pandas 3.0.2, scikit-learn 1.8.0, xgboost 3.2.0,
#   scipy 1.17.1, matplotlib 3.10.8, joblib 1.5.3, PyYAML 6.0.3,
#   yfinance 1.7.0, pytest 9.0.2
# The floors below are the oldest versions the code is written against.

# Data handling
numpy>=1.24
pandas>=2.0

# Modelling
scikit-learn>=1.3
xgboost>=2.0
scipy>=1.11            # Wilson intervals, binomial and McNemar tests

# Persistence and configuration
joblib>=1.3
PyYAML>=6.0

# Figures written by train.py under results/pipeline/
matplotlib>=3.7

# Price downloads. run_experiment.py reads data/NVDA_adjusted.csv and train.py
# reads the snapshots under data/raw, so normal runs never touch the network;
# yfinance is needed only to refresh either cache.
yfinance>=0.2.28

# Testing
pytest>=7.4
