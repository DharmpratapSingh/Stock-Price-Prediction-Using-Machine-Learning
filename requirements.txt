# Core — enough to run: python run_experiment.py
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28
scikit-learn>=1.3.0
xgboost>=2.0.0
scipy>=1.11.0
pyyaml>=6.0
joblib>=1.3.0

# Tests
pytest>=7.4.0

# Optional: older train.py / visualize path (not required for the lean experiment)
matplotlib>=3.7.0
seaborn>=0.12.0
